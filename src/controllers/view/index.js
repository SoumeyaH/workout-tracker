const renderExercises = require("./renderExercises");
const renderHome = require("./renderHome");
const renderStats = require("./renderStats");

module.exports = {
  renderExercises,
  renderHome,
  renderStats,
};
